#pragma once

/*
#include "../interfaces.h"
#include "../state.h"

class TMenuState : public TState
{
private:
	TViewInterface* MenuView;
public:
	TMenuState(TGameInterface* SetOwner);

	// from TStateInterface
	void OnActivate() override;
	void OnDeactivate() override;
	bool ProcessInput(TInputID InputID, float Value) override;
};
*/